import { Box, styled } from '@mui/material'

export const Image = styled('img')`
  width: 64px;
  height: 64px;
`

export const WeatherInfoContainer = styled(Box)`
  display: flex;
  align-items: center;
  gap: 10px;
`
